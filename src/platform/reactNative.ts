/**
 * React Native platform shims — re-exports for backward-compatible test hooks.
 * Collection logic lives in reactNativeRuntimeProvider.ts.
 */

export {
  resetPlatformMock,
  setPlatformMock,
  reactNativeRuntimeContextProvider,
} from './reactNativeRuntimeProvider';
